# OpenAI API key for the war-room copilot (Tab 2).
# Without a key the app still works: the copilot falls back to a local simulated narrator.
OPENAI_API_KEY=sk-...
# Optional: override the model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
